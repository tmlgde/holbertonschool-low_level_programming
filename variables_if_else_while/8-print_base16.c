#include <stdio.h>
#include <stddef.h>
/**
 * main - Prints a programming quote
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char a;
	char b;

	for (a = '0'; a <= '9'; a++)

	{
		putchar(a);
	}

	for (b = 'a'; b < 'g'; b++)

	{
		putchar (b);
	}
	putchar('\n');
		return (0);
}
