#include <stdio.h>
/**
 * main - point d'entrée du programme
 *
 * Description: Ce programme affiche exactement la phrase suivante :
 * puis retourne 0 pour indiquer que tout s'est bien passé.
 *
 * Return: 0 si tout se passe bien
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
